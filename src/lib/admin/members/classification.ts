import { deriveTrialState, type TrialState } from "@/lib/subscriptions/trial-state";
export type MemberClassificationInput = { nowMs:number; createdAt:string; trialEndsAt:string|null; trialStartedAt:string|null; foundingEligible:boolean; foundingPriceLocked:boolean; foundingPriceCents:number|null; foundingCurrency:string|null; paid:boolean; paidAt:string|null };
export type MemberClassification = MemberClassificationInput & { trial: TrialState; isNew:boolean; trialStatus:"active"|"expiring"|"expired"|"none"; foundingStatus:"founding-paid"|"founding-unpaid"|"none"; paymentStatus:"paid"|"unpaid"; convertedDuringTrial:boolean };
export function classifyMember(input:MemberClassificationInput):MemberClassification { const trial=deriveTrialState({nowMs:input.nowMs,trialEndsAt:input.trialEndsAt,foundingEligible:input.foundingEligible,foundingPriceLocked:input.foundingPriceLocked,paid:input.paid}); return {...input,trial,isNew:Date.parse(input.createdAt)>=input.nowMs-86400000,trialStatus:trial.trialActive?(trial.trialExpiring?"expiring":"active"):(trial.trialExpired?"expired":"none"),foundingStatus:input.foundingEligible?(input.paid?"founding-paid":"founding-unpaid"):"none",paymentStatus:input.paid?"paid":"unpaid",convertedDuringTrial:Boolean(input.paid&&input.paidAt&&input.trialEndsAt&&Date.parse(input.paidAt)<Date.parse(input.trialEndsAt))}; }

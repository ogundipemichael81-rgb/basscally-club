"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export function JoinForm(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[show,setShow]=useState(false);const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();if(password!==confirm){setError("Passwords must match.");return}setLoading(true);setError("");try{const response=await fetch("/api/join/create-account",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await response.json();if(!response.ok)throw new Error(data.error);const{data:signInData,error:signInError}=await createClient().auth.signInWithPassword({email:email.trim().toLowerCase(),password});if(signInError||!signInData.session)throw new Error("Your account was created. Sign in to continue.");window.location.replace("/dashboard?welcome=1")}catch(err){setError(err instanceof Error?err.message:"Could not create account.");setLoading(false)}}return <form onSubmit={submit} className="space-y-4"><label className="block text-sm">Email address<input className="mt-2 w-full rounded border p-3 text-base text-[var(--color-text)] [color-scheme:dark]" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm">Password<div className="flex gap-2"><input className="mt-2 min-w-0 flex-1 rounded border p-3 text-base text-[var(--color-text)] [color-scheme:dark]" type={show?"text":"password"} autoComplete="new-password" minLength={12} required value={password} onChange={e=>setPassword(e.target.value)}/><button className="mt-2 min-h-11 shrink-0 px-3" type="button" onClick={()=>setShow(!show)}>{show?"Hide":"Show"}</button></div></label><label className="block text-sm">Confirm password<input className="mt-2 min-w-0 flex-1 rounded border p-3 text-base text-[var(--color-text)] [color-scheme:dark]" type={show?"text":"password"} autoComplete="new-password" minLength={12} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><p className="text-sm text-[var(--color-text-muted)]">Use at least 12 characters. This email becomes your login. No card. No trial. Full Hub access.</p>{error?<p role="alert" className="text-sm text-red-400">{error}</p>:null}<button className="min-h-11 w-full rounded bg-[var(--color-brand)] px-4 py-3 font-bold text-[var(--color-text)] disabled:opacity-60" disabled={loading}>{loading?"Creating account…":"Join Basscally Hub"}</button></form>}
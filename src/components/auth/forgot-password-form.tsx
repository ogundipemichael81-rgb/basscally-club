"use client";
import { useState } from "react";
import Link from "next/link";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { createClient } from "@/lib/supabase/client";
export function ForgotPasswordForm(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const{error}=await createClient().auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:`${window.location.origin}/auth/callback?type=recovery`});if(error)setError(error.status===429?"Too many attempts. Please wait a moment and try again.":"We could not send recovery instructions right now.");else setSent(true);setLoading(false)}return <div className="relative z-[1] w-full max-w-[400px]"><h1 className="mb-3 text-2xl font-bold">Reset your password</h1><p className="mb-6 text-sm leading-relaxed text-[var(--color-text-muted)]">Enter your email and we&apos;ll send password recovery instructions if an account exists.</p>{sent?<><p className="rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] p-4 text-sm text-[var(--color-text-muted)]">If an account exists for this email, recovery instructions have been sent.</p><Link href="/auth/login" className="mt-5 inline-flex min-h-11 items-center text-[var(--color-brand)] underline">Return to sign in</Link></>:<form onSubmit={submit} className="space-y-4"><Input label="Email address" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/>{error?<p role="alert" className="text-sm text-[var(--color-danger)]">{error}</p>:null}<Button type="submit" className="w-full" disabled={loading}>{loading?"Sending...":"Send recovery email"}</Button></form>}{!sent?<Link href="/auth/login" className="mt-5 inline-flex min-h-11 items-center text-sm text-[var(--color-text-muted)] underline">Return to sign in</Link>:null}</div>}
import Link from "next/link";
import { redirect } from "next/navigation";
import { JoinForm } from "@/components/join/join-form";
import { getMemberSession } from "@/lib/subscriptions/member-session";
export default async function JoinPage(){const session=await getMemberSession();if(session)redirect("/dashboard");return <main className="mx-auto flex min-h-screen max-w-lg items-center px-5 py-12"><section className="w-full rounded-2xl border border-[var(--color-border)] bg-[var(--color-surface)] p-6 sm:p-8"><p className="text-sm text-[var(--color-brand)]">BASSCALLY HUB</p><h1 className="mt-3 text-3xl font-black">Create your Basscally Hub account</h1><p className="mt-3 text-[var(--color-text-muted)]">Basscally Hub is free to join. Create an account and get full access to every published practice drop.</p><p className="mt-2 text-sm text-[var(--color-text-dim)]">No card. No trial. Full Hub access.</p><div className="mt-7"><JoinForm /></div><p className="mt-5 text-sm text-[var(--color-text-muted)]">Already have an account? <Link className="text-[var(--color-brand)] underline" href="/auth/login">Sign in</Link></p></section></main>}
import { redirect } from "next/navigation";import { getMemberSession } from "@/lib/subscriptions/member-session";export default async function CheckoutCancelledPage(){redirect((await getMemberSession())?"/dashboard":"/join")}